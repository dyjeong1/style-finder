# 스타일매치 (가칭)

코디/착장 이미지를 업로드하면 AI가 이미지를 분석하고, 그 결과를 바탕으로 유사한 의류 상품을 추천해주는 로컬 중심 웹 서비스입니다.  
현재 제품 목표는 "현재 업로드한 이미지 1장"만 기준으로 추천을 만들되, 최근에 올린 이미지 파일은 이 브라우저에만 저장해 다시 분석할 수 있게 하는 흐름입니다.

---

## 0. 개발 착수 가이드

### 문서 운영 원칙
- 코딩 작업은 `PLAN` 단위로 시작합니다.
- 각 PLAN은 `TASKS`로 분해하고, 각 TASK에 `README.md`/`TODO.md`를 유지합니다.
- 루트 `README.md`/`TODO.md`는 작업 전후로 갱신합니다.

### 현재 기준 제품 원칙
- 업로드 1회당 분석 상태는 1개만 유지합니다.
- 새 이미지를 업로드하면 기존 업로드 분석 상태는 초기화합니다.
- 최근 업로드 이미지는 이 브라우저에만 저장하고, 다시 누르면 새 분석을 수행합니다.
- 추천은 AI 비전 분석 결과를 기준으로 생성합니다.
- 추천 상단의 현재 업로드 이미지는 썸네일 클릭으로 확대해 다시 확인할 수 있습니다.
- 규칙 분석기는 AI 실패 시에만 사용하는 마지막 fallback 으로 제한합니다.
- AI가 정상 응답한 경우 추천 검색은 `detected_items`/`category_query_hints`를 그대로 사용하고, 네이버 후보는 같은 카테고리 안에서 과하게 버리지 않습니다.
- 로컬 저장은 위시리스트와 최근 업로드 이미지처럼 사용자 의도가 있는 데이터에만 최소화하고, 업로드 분석 상태는 영속 저장하지 않습니다.
- 업로드/추천/위시리스트 상단에는 중복 요약 배지보다 핵심 제목과 필터 조작에 집중합니다.

### 최근 주요 PLAN
- `PLAN-20260508-AI검색직결및후처리완화`
- 상세 문서: `PLAN/PLAN-20260508-AI검색직결및후처리완화/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260508-AI검색직결및후처리완화/SPEC.md`
- 상태: `done` (AI 성공 시 규칙 기반 `bag` 보강을 제거하고, 네이버 후보 필터를 카테고리 중심으로 완화함, 2026-05-08)
  - 추가 메모: 추천 검색은 `analysis.category_query_hints`의 AI query를 그대로 사용함
  - 추가 메모: 네이버 후보는 카테고리만 맞으면 세부 품목/의도 mismatch 때문에 내부에서 버리지 않음
- `PLAN-20260507-악세사리추천노출보강`
- 상세 문서: `PLAN/PLAN-20260507-악세사리추천노출보강/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260507-악세사리추천노출보강/SPEC.md`
- 상태: `done` (전체 추천 상위 제한에서도 감지된 악세사리 카테고리 상품이 최소 1개는 남도록 보강함, 2026-05-07)
  - 추가 메모: `category_query_hints`가 있는 카테고리를 우선 보장 대상으로 사용함
  - 추가 메모: 카테고리 필터 추천은 기존처럼 단순 점수순 결과를 유지함
- `PLAN-20260506-추천분석패널핵심항목만노출`
- 상세 문서: `PLAN/PLAN-20260506-추천분석패널핵심항목만노출/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260506-추천분석패널핵심항목만노출/SPEC.md`
- 상태: `done` (추천 분석 패널에서 `감지 카테고리`, `감지 품목`만 남기고 보조 텍스트를 제거함, 2026-05-06)
  - 추가 메모: `AI 분석 기준`, 설명 문구, `검색 힌트`는 화면에서 제거함
  - 추가 메모: 감지 데이터와 추천 계산 로직은 그대로 유지함
- `PLAN-20260506-추천분석스타일칩숨김`
- 상세 문서: `PLAN/PLAN-20260506-추천분석스타일칩숨김/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260506-추천분석스타일칩숨김/SPEC.md`
- 상태: `done` (추천 분석 패널에서 내부 스타일 칩과 도출 설명 문구를 숨김, 2026-05-06)
  - 추가 메모: `톤/색상/무드/실루엣` 값은 추천 내부 계산에는 유지하고 사용자 화면에서만 감춤
  - 추가 메모: 감지 카테고리, 감지 품목, 검색 힌트는 그대로 유지함
- `PLAN-20260506-추천업로드이미지상단오버레이정리`
- 상세 문서: `PLAN/PLAN-20260506-추천업로드이미지상단오버레이정리/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260506-추천업로드이미지상단오버레이정리/SPEC.md`
- 상태: `done` (추천 기준 업로드 이미지 확대 보기를 dimmed 모달에서 상단 오버레이 카드로 정리함, 2026-05-06)
  - 추가 메모: 확대 보기는 이제 배경을 어둡게 덮지 않고 화면 상단에 뜨는 카드로 표시됨
  - 추가 메모: 카드 우측 상단의 `X` 버튼과 `Escape` 입력으로 닫을 수 있음
- `PLAN-20260506-추천분석노출정리및가방보강`
- 상세 문서: `PLAN/PLAN-20260506-추천분석노출정리및가방보강/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260506-추천분석노출정리및가방보강/SPEC.md`
- 상태: `done` (추천 페이지 상단 검색어 줄 제거, 분석 칩 도출 근거 설명 추가, AI 누락 가방 보강 정리 완료, 2026-05-06)
  - 추가 메모: 추천 상단의 자동 생성 `검색어:` 줄은 제거하고, 직접 입력 검색어 상태만 필터 패널 안에서 유지함
  - 추가 메모: `톤/색상/무드/실루엣` 칩 아래에 대표 색상/감지 품목 조합 기반 계산 설명을 추가함
  - 추가 메모: `resolve_detected_items`는 AI가 `bag`을 놓친 경우 규칙 분석의 `bag` 1건만 제한 보강함
- `PLAN-20260505-브랜드로고검색어반영`
- 상세 문서: `PLAN/PLAN-20260505-브랜드로고검색어반영/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-브랜드로고검색어반영/SPEC.md`
- 상태: `done` (로고가 명확히 보이는 경우 AI 검색어 앞에 브랜드를 반영함, 2026-05-05)
  - 추가 메모: AI 비전 응답은 선택적 `brand` 필드를 받아 `브랜드 + 색상 + 패턴 + 소재 + 품목` 순서 query를 지원함
  - 추가 메모: `new balance` 같은 영문 응답도 `뉴발란스`로 정규화하고, API 응답 `detected_items`에 `brand`가 함께 노출됨
- `PLAN-20260505-소재검색어사전확장`
- 상세 문서: `PLAN/PLAN-20260505-소재검색어사전확장/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-소재검색어사전확장/SPEC.md`
- 상태: `done` (AI 검색어의 소재 descriptor 사전을 확장해 검색 정밀도를 보강함, 2026-05-05)
  - 추가 메모: `쉬폰`, `트위드`, `벨벳`, `코듀로이`, `퍼` alias를 추가함
  - 추가 메모: `레이스`는 상의 query에서도 `패턴 + 소재` 흐름에 자연스럽게 남도록 정렬 순서를 보강함
- `PLAN-20260505-네이버호출정확도튜닝`
- 상세 문서: `PLAN/PLAN-20260505-네이버호출정확도튜닝/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-네이버호출정확도튜닝/SPEC.md`
- 상태: `done` (네이버 쇼핑 API 공식 파라미터 기반 호출 정확도 튜닝 완료, 2026-05-05)
  - 추가 메모: 네이버 쇼핑 호출은 이제 `sort`, `filter`, `exclude`를 설정값으로 반영함
  - 추가 메모: 기본 exclude로 `used:rental:cbshop`을 적용해 중고/렌탈/해외직구 노이즈 후보를 요청 단계에서 줄임
- `PLAN-20260505-모델의도보존추천`
- 상세 문서: `PLAN/PLAN-20260505-모델의도보존추천/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-모델의도보존추천/SPEC.md`
- 상태: `done` (모델 query/item_label 의도를 후보 필터와 랭킹까지 보존하는 1차 semantic 매칭 완료, 2026-05-05)
  - 추가 메모: `롱슬리브 ↔ 긴팔`, `숏슬리브 ↔ 반팔`, `슬리브리스 ↔ 민소매`를 같은 의도로 읽도록 연결함
  - 추가 메모: 추천 응답 `matched_signals`에 `target_intent_keywords`가 추가되어 모델 의도가 무엇이었는지 추적 가능함
- `PLAN-20260505-추천랭킹가중치분리`
- 상세 문서: `PLAN/PLAN-20260505-추천랭킹가중치분리/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-추천랭킹가중치분리/SPEC.md`
- 상태: `done` (추천 점수식 설정 분리와 품목명/비전 유사도 가중치 분리 완료, 2026-05-05)
  - 추가 메모: 추천 점수는 이제 설정 객체로 관리되고 `score_breakdown`에 `item_label_bonus`가 함께 노출됨
  - 추가 메모: 비전 이미지 유사도는 독립 가중치로 조절 가능하고, `matched_signals`에 카테고리별 `target_item_label`이 추가됨
- `PLAN-20260505-AI검색어고도화`
- 상세 문서: `PLAN/PLAN-20260505-AI검색어고도화/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-AI검색어고도화/SPEC.md`
- 상태: `done` (AI 검색어의 패턴/소재 descriptor 보존과 정규화 순서 고도화 완료, 2026-05-05)
  - 추가 메모: AI 검색어는 이제 가능하면 `색상 + 패턴 + 소재 + 품목` 순서를 사용함
  - 추가 메모: `무지` 표현은 사용자 노출 query에서 `민무늬`로 정규화하고, `체크`, `실크`, `새틴` 같은 descriptor 보존 범위를 확장함
- `PLAN-20260505-추천업로드이미지확대미리보기`
- 상세 문서: `PLAN/PLAN-20260505-추천업로드이미지확대미리보기/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-추천업로드이미지확대미리보기/SPEC.md`
- 상태: `done` (추천 상단 업로드 썸네일을 클릭 가능한 확대 미리보기 모달로 연결함, 2026-05-05)
  - 추가 메모: 추천 상단 썸네일을 누르면 현재 추천 기준 업로드 이미지를 큰 정사각형 모달로 다시 확인할 수 있음
  - 추가 메모: 닫기 버튼, 배경 클릭, `Escape` 입력으로 확대 보기를 닫을 수 있음
- `PLAN-20260505-추천업로드이미지프리뷰복원`
- 상세 문서: `PLAN/PLAN-20260505-추천업로드이미지프리뷰복원/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260505-추천업로드이미지프리뷰복원/SPEC.md`
- 상태: `done` (추천 메뉴 재진입 시 업로드 기준 유지와 업로드 원본 프리뷰 복원을 보강함, 2026-05-05)
  - 추가 메모: 최근 업로드 브라우저 저장소에 최신 `uploaded_image_id` 매핑을 함께 저장해 추천 메뉴와 프리뷰가 같은 기준을 다시 읽을 수 있게 함
  - 추가 메모: 추천 상단 썸네일과 확대 모달은 브라우저 저장소 원본을 우선 사용하고, 없을 때만 서버 파일 URL과 fallback 썸네일을 사용함
- `PLAN-20260506-추천빈업로드프리뷰숨김`
- 상세 문서: `PLAN/PLAN-20260506-추천빈업로드프리뷰숨김/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260506-추천빈업로드프리뷰숨김/SPEC.md`
- 상태: `done` (추천 페이지에서 업로드 없는 빈 상태의 프리뷰 카드를 숨기고, 분석 완료 뒤에만 다시 노출되게 정리함, 2026-05-06)
  - 추가 메모: 추천 상단 프리뷰와 확대 모달은 이제 `uploaded_image_id`와 `analysis`가 함께 준비된 경우에만 렌더링됨
  - 추가 메모: 업로드 전/후 프리뷰 노출 차이는 Playwright smoke 테스트로 고정함
- `PLAN-20260504-최근업로드이미지재사용복원`
- 상세 문서: `PLAN/PLAN-20260504-최근업로드이미지재사용복원/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260504-최근업로드이미지재사용복원/SPEC.md`
- 상태: `done` (분석 결과를 저장하지 않으면서 최근 업로드 이미지 재사용 UI를 복원함, 2026-05-04)
  - 추가 메모: 최근 업로드 이미지는 브라우저 `IndexedDB`에만 저장되고, 카드 클릭 시 저장된 이미지를 다시 업로드해 새 분석을 시작함
  - 추가 메모: 최근 업로드 카드는 개별 삭제할 수 있고 최신 8개까지만 유지함
  - 추가 메모: 업로드 화면 폭이 충분하면 최근 업로드 패널을 우측 세로 컬럼으로 배치하고, 좁은 화면에서만 아래로 내려감
  - 추가 메모: 최근 업로드 패널의 저장/재분석 보조 문구는 제거하고 제목과 빈 상태 핵심 문장만 남김
- `PLAN-20260504-업로드로딩및소재검색강화`
- 상세 문서: `PLAN/PLAN-20260504-업로드로딩및소재검색강화/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260504-업로드로딩및소재검색강화/SPEC.md`
- 상태: `done` (업로드 진행 시간 노출과 추천 상단 업로드 썸네일 표시까지 완료, 2026-05-04)
  - 추가 메모: 업로드 버튼 내부 스피너는 제거했고, 버튼 아래에는 실시간 `분석 진행 시간` 문구를 노출함
  - 추가 메모: 비전 분석 검색어는 보이는 소재가 있으면 `색상 + 소재 + 품목` 형태를 우선 사용하도록 확장함
  - 추가 메모: 빠른 업로드 응답에서도 스피너가 한 번은 보이도록 다음 페인트 대기와 최소 노출 시간을 적용함
  - 추가 메모: 추천 화면 상단에는 현재 업로드 이미지를 작은 정사각형 썸네일로 함께 보여줘 추천 기준 이미지를 바로 확인할 수 있음
- `PLAN-20260504-프론트원복`
- 상세 문서: `PLAN/PLAN-20260504-프론트원복/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260504-프론트원복/SPEC.md`
- 상태: `done` (최근 프론트 리디자인/고급화 변경을 제거하고 `0cd7e00` 기준의 이전 안정 UI 상태로 복구 완료, 2026-05-04)
- `PLAN-20260503-업로드AI추천흐름단순화`
- 상세 문서: `PLAN/PLAN-20260503-업로드AI추천흐름단순화/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260503-업로드AI추천흐름단순화/SPEC.md`
- 상태: `done` (업로드 단일 세션화, AI 우선 추천, fallback 최소화, local smoke/CI 검증, 문서 마감을 완료함, 2026-05-04)
  - 추가 메모: 브랜드 보조 문구를 `이미지 기반 스타일 추천`으로 정리하고 업로드 화면 메인 헤드라인을 제거함
  - 추가 메모: 추천/위시리스트 상단의 중복 요약 카드 묶음을 제거해 필터 UI와 정보 중복을 줄임
  - 추가 메모: 추천 페이지가 새 `uploaded_image_id`로 바뀔 때 이전 검색어/필터/분석 요약이 남지 않도록 E2E 회귀 테스트를 보강함
  - 추가 메모: 로컬 Ollama 설치부터 `gemma3:4b` pull, `.env` 설정, 확인 명령까지 한 번에 따라갈 수 있는 운영 가이드를 추가함
  - 추가 메모: 프론트 기본 API 주소를 `http://127.0.0.1:8000`으로 정리하고 업로드 후 추천 이동을 하드 리다이렉트 fallback 으로 보강해 로컬 브라우저 테스트를 더 안정적으로 만듦
  - 추가 메모: 로컬 Ollama 주소가 닫혀 있으면 업로드 분석이 긴 timeout 동안 멈추지 않도록 빠른 연결 확인 뒤 즉시 규칙 fallback 으로 전환함
  - 추가 메모: 추천 페이지는 production build에서도 `useSearchParams()`가 비어 있는 순간 현재 `window.location.search`를 함께 읽어 `uploaded_image_id`를 놓치지 않도록 보강함
  - 추가 메모: Playwright 회귀 테스트는 이제 dev 서버뿐 아니라 `next build + next start` 기반 local 모드로도 실행할 수 있고, 핵심 `uploaded_image_id` 동기화 회귀가 local 모드에서 통과함
  - 추가 메모: 업로드 핵심 흐름과 추천 URL 동기화는 `@smoke` 태그와 `npm run test:e2e:smoke`, `npm run test:e2e:local:smoke` 스크립트로 dev/local 모두 빠르게 재검증할 수 있고, local smoke 2개 시나리오가 통과함
  - 추가 메모: `Frontend E2E` GitHub Actions 워크플로우에 `next start` 기반 local smoke job을 추가해 업로드/추천 핵심 회귀를 CI에서도 함께 검증하도록 연결 완료함
- `PLAN-20260423-검색어하드코딩제거`
- 상세 문서: `PLAN/PLAN-20260423-검색어하드코딩제거/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260423-검색어하드코딩제거/SPEC.md`
- 상태: `done` (검색어 하드코딩 제거 완료, 2026-04-23)
- `PLAN-20260423-동적업로드분석및카테고리감지`
- 상세 문서: `PLAN/PLAN-20260423-동적업로드분석및카테고리감지/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260423-동적업로드분석및카테고리감지/SPEC.md`
- 상태: `done` (이미지별 동적 분석, 없는 카테고리 제외, 악세서리 감지 반영 완료, 2026-04-24)
- `PLAN-20260424-품목세분화및비전재정렬`
- 상세 문서: `PLAN/PLAN-20260424-품목세분화및비전재정렬/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260424-품목세분화및비전재정렬/SPEC.md`
- 상태: `done` (품목 세분화, 다중 아이템 분리, 선택적 CLIP 비전 재정렬 연결 완료, 2026-04-24)
- `PLAN-20260424-카테고리오탐억제및아우터정교화`
- `PLAN-20260424-AI비전기반착장분석도입`
- 상세 문서: `PLAN/PLAN-20260424-AI비전기반착장분석도입/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260424-AI비전기반착장분석도입/SPEC.md`
- 상태: `doing` (AI 비전 착장 분석 기반은 유지하되, 현재는 `PLAN-20260503-업로드AI추천흐름단순화` 기준으로 런타임과 상태 관리를 재정리하는 단계, 2026-05-03)
  - 추가 메모: 사용자 제공 실제 코디 이미지 10장을 비전 데이터셋에 반영하고 라벨 초안을 작성함
  - 추가 메모: 사용자 검수 내용을 반영해 일부 라벨을 보정함
  - 추가 메모: codytest_9 outer를 버건디 가디건으로 재보정함
  - 추가 메모: 평가 스크립트로 현재 규칙 기반 분석기의 정밀도 0.2500, 재현율 0.2281을 확인함
  - 추가 메모: `OPENAI_API_KEY`, `OPENAI_VISION_*` 환경변수와 호환되는 OpenAI Vision provider를 연결함
  - 추가 메모: 개인 비상업 프로젝트용 무료 경로로 Gemini Vision provider와 `GEMINI_*` 환경변수 호환을 추가함
  - 추가 메모: Gemini 실호출 검증에서 `codytest_2` 기준 `하늘색 라운드넥 가디건 / 화이트 나시탑 / 블랙 스티치 와이드 팬츠 / 블랙 안경테 / 블랙 링 귀걸이` 결과를 확인함
  - 추가 메모: 규칙 기반 vs Gemini 비교 스크립트와 캐시 구조를 추가했고, 무료 티어 한도로 오늘은 3샘플 캐시까지 확보함
  - 추가 메모: 로컬 무료 반복 실험용으로 Ollama provider와 `gemma3:4b` 기본 모델 경로를 추가함
  - 추가 메모: 현재 작업 머신에는 Ollama가 설치되어 있지 않아 실모델 응답 대신 연결 거부 fallback 흐름만 검증함
  - 추가 메모: qwen 기준 timeout/부분 실행/단일 업로드 확인 CLI를 추가했고, 현재 맥북 환경에서는 더 가벼운 모델 전환이 합리적임을 확인함
  - 추가 메모: Ollama 로컬 기본 모델을 `gemma3:4b`로 전환했고, `codytest_2` 실응답과 단일 샘플 비교 실행까지 확인함
  - 추가 메모: `gemma3:4b` 후처리 정규화를 추가해 `블루 데님 팬츠`, `블랙 안경` 같은 서비스형 품목명으로 정리되도록 보정함
  - 추가 메모: 3-way 비교 결과 현재 순위는 `gemini > ollama(gemma3:4b) > rule`이며, 상의/아우터 1차 정규화와 `--no-cache` 비교 옵션을 추가함
  - 추가 메모: AI 결과와 규칙 기반 fallback을 같은 품목 계열에서 재조정하는 하이브리드 병합 보정을 추가했고, `codytest_2`는 `화이트 셔츠 / 블루 가디건 / 블랙 데님 팬츠 / 블랙 안경`으로 개선됨
  - 추가 메모: Ollama 결과가 레이어드/색상 충돌을 보일 때만 Gemini를 보조 호출하는 선택적 보정 경로를 추가했고, `codytest_2`는 `화이트 슬리브리스 탑 / 블루 가디건 / 블랙 와이드 팬츠 / 블랙 안경` 수준으로 개선됨
  - 추가 메모: 가방/하의 generic 품목 정교화를 적용해 `codytest_3`는 `브라운 숄더백 / 화이트 스니커즈 / 브라운 팬츠` 수준으로 더 구체화됨
  - 추가 메모: 액세서리 세부명/색상 정교화를 적용해 `codytest_3`는 `그레이 팔찌 / 그레이 목걸이` 수준으로 더 자연스럽게 정리됨
  - 추가 메모: 액세서리 우선순위와 금속 톤 검색어를 정리해 `codytest_3`는 `실버 목걸이 / 실버 팔찌 / 실버 반지 / 화이트 양말` 순으로 정리됨
  - 추가 메모: 비전 평가 캐시에 런타임 정규화를 적용하고 스트라이프 니트/네이비 데님/미니 스커트 품목 보정을 추가해 Gemini 비교 정밀도/재현율을 0.6316까지 개선함
  - 추가 메모: 추천 페이지가 `uploaded_image_id` 변경을 계속 반영하고 업로드 ID 변경 시 이전 검색어/직접 입력 검색어/필터/결과를 초기화하도록 보강함
  - 추가 메모: 이전 추천 API 응답이 새 업로드 결과를 덮지 않도록 요청 키를 검증하고, 컬러 의상에서 레이어드 베스트 고정 키워드가 반복되지 않도록 규칙 기반 특수 시그니처를 축소함
  - 추가 메모: 업로드 착장 분석은 AI 결과가 있으면 그 결과를 그대로 우선 사용하고, 규칙 분석기는 AI가 비어 있을 때만 fallback 으로 축소함
  - 추가 메모: 데이터셋 비교 스크립트에서도 실제 런타임 AI-first 파이프라인(`runtime-ollama+gemini`)을 직접 측정할 수 있게 정렬했고, 캐시 기준 `precision 0.6111 / recall 0.5789`를 확인함
  - 추가 메모: 검색어를 `색상 + 품목` 수준에서 `블랙 레더 자켓`, `블랙 도트 미니 스커트`처럼 더 구체화했고, 샘플별 상세 리포트 파일 출력까지 보강함
  - 추가 메모: 추천 API 응답이 업로드 분석 요약을 함께 반환하고, 추천 페이지가 그 값을 기준으로 로컬 히스토리/검색 힌트를 다시 동기화하도록 보강해 다른 이미지 업로드 후 이전 검색어가 남는 회귀를 줄임
  - 추가 메모: 비전 분석 설정은 이제 실행 디렉터리와 무관하게 `backend/.env`를 읽고, `ollama`/`gemini`/`openai` provider별 모델 설정을 런타임에서 우선 반영함
  - 추가 메모: 안경/귀걸이 query는 `메탈 안경`, `진주 귀걸이`, `링 귀걸이`처럼 descriptor를 더 보존하고, `칼라` 같은 unsupported accessory 노이즈와 단일 추가 jewelry 보정 오탐은 더 보수적으로 걸러냄
  - 추가 메모: `backend/scripts/generate_openai_vision_report.py`로 OpenAI 비교 리포트를 `backend/data/vision_dataset/reports/openai/` 아래에 자동 생성할 수 있게 했고, runtime predictor 반환 버그도 함께 정리함
  - 추가 메모: 로컬 Ollama 미실행 시에는 `provider_unreachable`로 즉시 규칙 fallback 하도록 보강해 업로드 버튼이 오래 `이미지 분석 중...` 상태로 남는 흐름을 줄임
- 상세 문서: `PLAN/PLAN-20260424-카테고리오탐억제및아우터정교화/PLAN.md`
- 기술 스펙: `PLAN/PLAN-20260424-카테고리오탐억제및아우터정교화/SPEC.md`
- 상태: `done` (없는 카테고리 오탐 억제, 가디건 분리 정교화, 네이버 relevance 필터 보강, 실사 셀카 일반화 분석 개선 완료, 2026-04-24)
  - 추가 메모: 실사 셀카형 회귀 픽스처를 확장해 가방/신발 존재 여부와 부재 조건을 테스트로 고정함
  - 추가 메모: 하의 품목을 `데님 팬츠`, `와이드 데님 팬츠`, `슬랙스`, `스커트` 수준으로 세분화함
  - 추가 메모: 악세서리 품목을 `안경`, `모자`, `머플러`, `귀걸이` 수준으로 세분화하고 실사형 회귀 테스트를 추가함

### 현재 저장소 구조(초기)
```text
.
├─ AGENTS.md
├─ README.md
├─ TODO.md
├─ .sisyphus/
│  └─ plans/
│     └─ PLAN-20260331-MVP초기세팅.md
├─ PLAN/
│  └─ PLAN-20260331-MVP초기세팅/
│     ├─ PLAN.md
│     ├─ SPEC.md
│     └─ TASKS/
│        ├─ TSK-0001-저장소구조세팅/
│        │  ├─ README.md
│        │  └─ TODO.md
│        └─ TSK-0002-아키텍처초안정의/
│           ├─ README.md
│           └─ TODO.md
│  └─ PLAN-20260401-API상세화및구현준비/
│     ├─ PLAN.md
│     ├─ SPEC.md
│     └─ TASKS/
│        ├─ TSK-0001-OpenAPI상세화/
│        │  ├─ README.md
│        │  └─ TODO.md
│        ├─ TSK-0002-DB마이그레이션초안작성/
│        │  ├─ README.md
│        │  └─ TODO.md
│        └─ TSK-0003-백엔드스캐폴딩/
│           ├─ README.md
│           └─ TODO.md
└─ src/
   └─ name/
      └─ data/
```

### 설치/실행 (1차)
현재는 백엔드 기초, 프론트 MVP, E2E CI, `main` 브랜치 보호 규칙 적용, PR 기준 required check 실동작 검증, GitHub Actions Node 24 대응, solo 운영 기준 브랜치 보호 정책 정리, required check 이름 정합성 수정, 업로드 이미지 분석/추천 점수 고도화와 업로드 히스토리 연결, 로그인 없는 로컬 단일 사용자 모드 전환, 위시리스트 상세화, Pretendard 기반 프론트 비주얼 리프레시, 추천 저장 상태 연결, 추천 페이지 hydration mismatch 수정, 프론트 로컬 안정 실행 스크립트 추가, stale 업로드 상태 자동 복구, 위시리스트 파일 영속 저장, 위시리스트 정렬 옵션 추가, 업로드 화면 레퍼런스형 리디자인, 업로드 드래그앤드롭 추가, 네이버 쇼핑 연동 진단 표시, 최근 업로드 실제 썸네일 표시, 업로드 이미지 실제 응답 보강, 추천 카테고리 다양화, 추천 상품 카드 UX 정리, 추천 결과 카테고리 섹션화, 추천 카테고리 바로가기, 추천 검색어 직접 입력, 직접 검색어 카테고리 추론, 신발 검색어 추론 확장, 제품군 키워드 사전 확장, 이미지 색상 기반 추천 정확도 개선, 상품 이미지 직접 분석 추천 정확도 개선, 카테고리별 코디 검색어 추출, 검색어 하드코딩 제거, 이미지별 동적 업로드 분석 및 없는 카테고리 제외, 악세서리 감지, 품목 세분화, 연결 컴포넌트 기반 다중 아이템 분리, 선택적 CLIP 비전 재정렬 연결, 없는 카테고리 오탐 억제, 가디건/아우터 relevance 정교화, 악세서리 품목 세분화까지 완료됐고, AI 비전 착장 분석용 데이터셋 구조와 fallback 가능한 분석 인터페이스 초안, 데이터셋 기반 예측/정답 비교 평가 스크립트, OpenAI Vision provider 연결, Gemini 무료 티어 provider 연결, Ollama 로컬 Vision provider 경로까지 추가됐습니다.

1. 저장소 문서 확인: `README.md`, `AGENTS.md`, `TODO.md`
2. 최신 PLAN 확인: `PLAN/PLAN-20260423-검색어하드코딩제거/PLAN.md`
3. API/DDL 초안 확인:
   - `PLAN/PLAN-20260331-MVP초기세팅/TASKS/TSK-0002-아키텍처초안정의/openapi.yaml`
   - `PLAN/PLAN-20260331-MVP초기세팅/TASKS/TSK-0002-아키텍처초안정의/schema.sql`
4. 상세 OpenAPI 확인:
   - `docs/openapi/openapi.yaml`
5. DB 마이그레이션 확인:
   - `backend/migrations/V1__init_schema.sql`
   - `backend/migrations/R__rollback_v1.sql`
6. 백엔드 스캐폴딩 확인:
   - `backend/src/main.py`
   - `backend/src/api/routes/health.py`
   - `backend/pyproject.toml`
   - `backend/scripts/run-dev.sh`
7. API 통합 테스트 확인:
   - `backend/tests/test_api_e2e.py`
   - 실행: `cd backend && PYTHONPATH=. python3 -m pytest tests -q`
8. CI 테스트 워크플로우 확인:
   - `.github/workflows/backend-tests.yml`
   - `backend/requirements-test.txt`
9. 프론트 스캐폴딩 확인:
   - `frontend/app/(auth)/login/page.tsx` (`/upload`로 즉시 리다이렉트)
   - `frontend/app/(main)/upload/page.tsx`
   - `frontend/app/(main)/recommendations/page.tsx`
   - `frontend/app/(main)/wishlist/page.tsx`
   - `frontend/components/app-shell.tsx`
   - 권장 실행: `cd frontend && npm run local`
10. 로컬 Ollama 빠른 시작:
   - 상세 가이드: `docs/ollama-local-setup.md`
   - 권장 기본 모델: `gemma3:4b`
   - 최소 절차:
     - `brew install --cask ollama` 또는 공식 앱 설치
     - `ollama pull gemma3:4b`
     - `ollama serve`
     - `cd backend && cp .env.example .env`

     - `.env`에 `OLLAMA_VISION_ENABLED=true`, `OLLAMA_VISION_PROVIDER=ollama`, `OLLAMA_VISION_MODEL=gemma3:4b` 설정
     - `cd backend && PYTHONPATH=. python3 scripts/check_upload_analysis.py --image data/vision_dataset/images/codytest_2.jpg --provider ollama`
11. 프론트 API 연동 확인:
   - `frontend/lib/api.ts`
   - 업로드 직후 `/recommendations?uploaded_image_id=...`로 현재 업로드 ID만 전달
12. 프론트 보안 업그레이드 확인:
   - `frontend/package.json`
   - `frontend/package-lock.json`
   - `next@15.5.15`
13. 프론트 E2E 테스트 확인:
   - `frontend/playwright.config.ts`
   - `frontend/e2e/core-flow.spec.ts`
   - 실행: `cd frontend && npm run test:e2e`
14. 프론트 E2E CI 확인:
   - `.github/workflows/frontend-e2e.yml`
14. 브랜치 보호 준비 확인:
   - `.github/branch-protection/main.json`
   - `scripts/apply-branch-protection.sh`
   - `docs/github-branch-protection.md`
15. 브랜치 보호 적용 상태 확인:
   - GitHub 저장소 `dyjeong1/style-finder`의 `main` 브랜치에 보호 규칙 적용 완료
   - required check: `Backend Tests / test`, `Frontend E2E / e2e`
16. 브랜치 보호 실검증 확인:
   - PR #1: `https://github.com/dyjeong1/style-finder/pull/1`
   - `pull_request` 기준 백엔드/프론트 required check 생성 확인
17. GitHub Actions Node 24 대응 확인:
   - `actions/checkout@v5`
   - `actions/setup-python@v6`
   - `actions/setup-node@v5`
   - `actions/upload-artifact@v6`
18. 브랜치 보호 운영 정책 메모:
   - 현재 단독 운영 저장소 기준으로 리뷰 승인 수는 `0`
   - required check는 유지하고 승인 병목만 제거
19. required check 정합성 메모:
   - required check는 `Backend Tests / test (pull_request)`, `Frontend E2E / e2e (pull_request)` 기준으로 조정
20. 업로드 분석/추천 점수 메모:
   - 업로드 응답에 `analysis` 요약이 포함됨
   - 추천 응답은 upload feature vector 기반 `score_breakdown`과 `matched_signals`를 포함함
21. 프론트 분석 노출 메모:
   - `/upload`에서 업로드 직후 Quick Analysis 확인 가능
   - `/recommendations`에서 Upload Analysis와 상품별 score breakdown 확인 가능
22. 업로드 단일 세션 메모:
   - `/upload`는 현재 선택한 이미지 1건만 기준으로 분석과 추천 이동을 처리함
   - 업로드 ID/분석 결과/업로드 히스토리는 브라우저 localStorage에 저장하지 않음
   - 대신 최근 업로드 이미지 파일만 브라우저 `IndexedDB`에 저장해 재분석에 사용함
23. AI 우선 추천 기준 메모:
   - 업로드 분석 응답은 `analysis_source`, `query_source`, `fallback_reason` 메타를 포함해 AI 분석 기준과 fallback 사용 여부를 함께 반환함
   - 추천 페이지는 `AI 분석 기준` 또는 `fallback 분석 기준`을 직접 표시해 현재 추천 근거를 드러냄
   - 감지 품목이 비어 있을 때는 digest 기반 임의 카테고리 fallback 을 더 이상 사용하지 않음
   - AI가 빈 결과를 돌려준 경우에는 그대로 AI 결과를 사용하고, provider unavailable/error 시에만 규칙 fallback 을 사용함
24. 단일 사용자 모드 메모:
   - 백엔드 업로드/추천/찜 API는 로그인 없이 바로 호출 가능
   - 위시리스트는 로컬 단일 사용자 기준으로 저장됨
25. 위시리스트 상세화 메모:
   - 위시리스트 응답에 상품명, 가격, 쇼핑몰, 카테고리, 링크가 포함됨
   - 저장 시각이 재조회 후에도 유지됨
   - 카드형 썸네일 UI와 이미지 실패 시 대체 썸네일이 적용됨
26. 프론트 비주얼 리프레시 메모:
   - Pretendard가 전역 폰트로 적용됨
   - 공통 네비게이션과 배경이 유리 질감/웜 톤 중심으로 정리됨
   - 업로드/추천/위시리스트 화면에 페이지 헤더와 카드 썸네일 위계가 강화됨
27. 추천 저장 상태 연결 메모:
   - 추천 카드에서 이미 저장된 상품에 `Saved` 배지가 표시됨
   - 저장된 상품은 버튼이 비활성화되어 중복 찜을 막음
   - 저장 직후 추천 화면 요약과 카드 상태가 즉시 갱신됨
28. 프론트 상태 정합성 메모:
   - 추천 페이지는 URL의 `uploaded_image_id`가 바뀌면 현재 업로드 기준으로 필터/검색 상태를 초기화함
   - 이전 업로드 로컬 상태를 제거해 업로드 전환 시 오래된 분석 요약이 남지 않도록 정리함
29. 프론트 로컬 실행 안정화 메모:
   - `npm run local`이 `build + start --hostname 127.0.0.1 --port 3000`를 한 번에 실행함
   - 로컬 화면 확인은 `next dev`보다 `npm run local`을 기본 경로로 권장함
   - `./scripts/start-local-stack.sh`, `./scripts/status-local-stack.sh`, `./scripts/stop-local-stack.sh`를 기준 실행 경로로 유지하고 `.local-runtime/` 로그/PID는 다시 git 추적에서 제외함
   - 업로드 원본 파일명이 한글이어도 `/images/{upload_id}/file` 응답이 깨지지 않도록 `Content-Disposition` 헤더를 UTF-8 방식으로 보강함
30. 추천 stale 업로드 상태 복구 메모:
   - 백엔드 재시작 후 이전 `uploaded_image_id`가 남아 있어도 추천 페이지가 치명적으로 깨지지 않음
   - stale 업로드 상태를 자동 초기화하고 `/upload` 재업로드를 안내함
31. 위시리스트 영속 저장 메모:
   - 위시리스트는 `backend/data/wishlist.json`에 자동 저장됨
   - 백엔드 재시작 후에도 Saved 상태와 위시리스트 목록이 유지됨
32. 위시리스트 정렬 메모:
   - 위시리스트에서 `latest`, `oldest`, `price_asc`, `price_desc`, `name_asc` 정렬을 지원함
   - 정렬 변경은 추가 API 호출 없이 프론트에서 즉시 반영됨
33. 업로드 화면 리디자인 메모:
   - `/upload`는 큰 인트로 패널과 독립된 드롭존 중심 구조로 재구성됨
   - 메인 CTA는 `이미지 분석하기` 버튼으로 강조되고, 보조 재사용 패널 없이 현재 업로드 흐름에 집중함
34. 업로드 드래그앤드롭 메모:
   - 드롭존에 이미지 파일을 끌어다 놓아 바로 선택할 수 있음
   - 드래그 중에는 드롭존이 강조 색상과 그림자로 활성 상태를 보여줌
35. 업로드 문구 단순화 메모:
   - 업로드 화면의 영어 배지와 보조 문구를 제거해 첫 화면을 더 간결하게 정리함
   - 당시에는 현재 업로드 1건 흐름만 남기고 최근 업로드 재사용 UI를 제거했음
   - 메인 타이틀 폰트 크기를 낮춰 상단 헤드라인의 시각적 무게를 줄임
36. 프론트 폰트 단위 pt 전환 메모:
   - `frontend/app/globals.css`의 `font-size` 선언을 pt 기준으로 정리함
   - 업로드 화면 메인 타이틀은 요청값인 `36pt`로 고정함
   - 남아 있던 반응형 `clamp(...)` font-size는 모두 제거하고 고정 pt 값으로 통일함
   - 이후 업로드 화면 메인 타이틀은 `30pt`, CTA 버튼은 `14pt`와 더 작은 박스로 추가 조정함
37. 업로드 영역 통합 UX 메모:
   - 업로드 선택 영역과 미리보기 영역을 하나의 박스로 통합함
   - 선택한 이미지는 같은 영역 안에서 정사각형 미리보기로 노출됨
   - 삭제 버튼으로 선택 파일, 미리보기, 상태 메시지를 즉시 초기화할 수 있음
   - 통합 박스 안에서는 업로드 카피를 상단, 정사각형 미리보기는 하단으로 배치해 읽는 순서를 정리함
   - 안내 문구는 `클릭하거나 이미지를 끌어다 놓아 주세요.`로 다듬어 톤을 정리함
37. 브랜드 아이콘 교체 메모:
   - 좌측 상단 헤더 브랜드 마크를 제공된 로고 이미지로 교체함
   - 로고 자산은 `frontend/public/brand/stylefinder_logo.png`로 정리함
38. 헤더 상태 표시 정리 메모:
   - 우측 상단 `Local Mode` 배지를 제거함
39. 하이브리드 병합 보정 메모:
   - AI 비전 결과와 규칙 기반 fallback을 같은 품목 계열일 때만 제한적으로 병합해 색상/세부품목을 보정함
   - `top`으로 잘못 들어온 `가디건`은 fallback의 `outer`와 맞춰 재배치하고, `데님 팬츠` 계열은 더 구체적인 fallback 품목으로 보정할 수 있음
40. 선택적 Gemini 보정 메모:
   - 기본 업로드 분석은 계속 `Ollama + gemma3:4b`를 사용함
   - `top/outer` 레이어드 동시 존재, fallback과의 색상/품목명 충돌, generic 품목명 감지 시에만 Gemini를 보조 호출함
   - 보정 대상은 `top`, `outer`, `bottom`, `accessory`로 제한해 quota 사용량을 줄임
41. 가방/하의 generic 품목 정교화 메모:
   - `운동화`는 `스니커즈`로 정규화해 신발 표현을 통일함
   - `가방`은 fallback이나 Gemini 결과에 더 구체적인 `숄더백/토트백/크로스백`이 있으면 그 표현을 우선 사용함
   - `바지/팬츠` 계열도 같은 그룹 내에서 더 구체적인 품목명이 있으면 그쪽으로 보정함
42. 액세서리 세부명/색상 정교화 메모:
   - `체인 팔찌 -> 팔찌`, `반지 -> 반지` 정규화를 추가함
   - `목걸이`, `귀걸이`, `팔찌`, `반지`처럼 작은 주얼리는 색상이 비어 있으면 기본적으로 `그레이`로 보정함
   - `실버`, `골드`, `메탈` 텍스트가 있으면 각각 `그레이`, `옐로우` 계열로 우선 매핑함
43. 액세서리 우선순위/금속톤 메모:
   - 주얼리 계열 `gray/yellow` 검색어는 사용자 노출 시 `실버/골드` 표현으로 바뀜
   - 액세서리 정렬은 `목걸이/귀걸이/팔찌`가 `반지/양말`보다 앞에 오도록 조정함
   - 업로드 보조 문구를 `이미지를 넣으면 유사한 상품을 추천해드립니다.`로 변경함
   - 업로드 박스에 `PNG, JPG, JPEG, WEBP` 허용 형식 안내를 추가함
39. 업로드 안내 및 최근 기록 정리 메모:
   - 허용 형식 문구를 이미지 placeholder 자리로 이동함
   - 당시 최근 업로드 카드별 `삭제` 버튼을 추가했음
- 이후 `PLAN-20260504-최근업로드이미지재사용복원`에서 localStorage 대신 `IndexedDB` 기반 최근 업로드 이미지 재사용 UI를 다시 복원함
40. 최근 업로드 카드 액션 정리 메모:
   - 당시 최근 업로드 카드 삭제 버튼을 카드 내부 우측 상단 `x` 아이콘으로 변경했음
   - placeholder 문구를 `허용 이미지: PNG, JPG, JPEG, WEBP`로 조정함
41. 업로드 미리보기 조건부 노출 메모:
   - 업로드 전에는 정사각형 미리보기 영역을 숨김
   - 파일을 선택한 뒤에만 정사각형 미리보기가 나타나도록 조정함
   - 허용 형식 문구는 업로드 안내 카피 아래에 유지함
42. 추천 카테고리 다양화 메모:
   - 추천 전체 보기에서 네이버 쇼핑 후보를 상의/하의/아우터/신발/가방으로 분산 조회함
   - 상품 ID 기준 중복 제거 후 유사도순으로 재정렬함
43. 추천 상품 카드 UX 정리 메모:
   - 추천 카드는 상품 이미지, 상품명, 가격, 상품 보기, 위시리스트 액션 중심으로 재구성됨
   - 출처/카테고리/랭킹은 이미지 위 배지로 정리함
   - 유사도, 톤, 무드, 실루엣, 점수 breakdown은 접을 수 있는 매칭 정보 영역으로 이동함
44. 추천 결과 카테고리 섹션화 메모:
   - 추천 전체 보기에서 상품을 상의/하의/아우터/신발/가방 섹션으로 묶어 표시함
   - 각 섹션 헤더에 상품 개수와 최고 매칭률을 표시함
   - 특정 카테고리 필터 선택 시 기존 단일 그리드 흐름을 유지함
45. 추천 카테고리 바로가기 메모:
   - 추천 전체 보기 상단에 카테고리별 요약/바로가기 바를 추가함
   - 요약 카드에는 카테고리명, 상품 개수, 최고 매칭률을 표시함
   - 요약 카드를 누르면 해당 카테고리 섹션으로 이동함
46. 추천 검색어 직접 입력 메모:
   - 추천 API가 `custom_query` 파라미터를 받아 네이버 쇼핑 검색어를 직접 지정할 수 있음
   - 전체 보기에서는 직접 검색어에 카테고리 키워드를 붙여 다양한 제품군 조회를 유지함
   - 추천 페이지 필터 영역에서 검색어 적용/초기화를 할 수 있음
47. 직접 검색어 카테고리 추론 메모:
   - 직접 검색어에 `신발`, `가방`, `아우터` 등 제품군 키워드가 있으면 해당 카테고리만 조회함
   - `검은색 신발` 검색 시 상의/하의 섹션용 신발 검색어를 만들지 않음
   - 제품군이 없는 검색어는 기존처럼 전체 카테고리 분산 조회를 유지함
48. 신발 검색어 추론 확장 메모:
   - `메리제인`, `로퍼`, `슬리퍼`, `구두`, `플랫`, `힐`, `펌프스`, `뮬` 등 신발 하위 품목을 신발 카테고리로 추론함
   - `블랙 메리제인` 검색 시 신발 섹션만 조회하도록 쿼리 생성을 제한함
49. 제품군 키워드 사전 확장 메모:
   - 상의: 셔츠, 니트, 블라우스, 맨투맨, 후드티, 민소매, 크롭티, 폴로, 베스트 등 하위 품목을 추론함
   - 하의: 청바지, 슬랙스, 와이드팬츠, 조거팬츠, 쇼츠, 스커트, 레깅스 등 하위 품목을 추론함
   - 아우터: 트렌치코트, 블레이저, 패딩, 바람막이, 무스탕, 플리스, 야상 등 하위 품목을 추론함
   - 가방: 호보백, 버킷백, 클러치, 에코백, 메신저백, 체인백, 보스턴백 등 하위 품목을 추론함

---

## 1. 프로젝트 소개

패션 이미지를 보고 “이 코디와 비슷한 옷을 어디서 살 수 있지?”라는 니즈는 많지만, 실제로는 사용자가 여러 쇼핑몰을 오가며 직접 검색해야 합니다.  
이 서비스는 코디 이미지를 기반으로 유사한 상품을 자동으로 탐색하고, 상품을 카테고리별로 나누어 추천함으로써 탐색 시간을 줄이고 구매 전환 가능성을 높이는 것을 목표로 합니다.

### 핵심 가치
- 코디 이미지 한 장으로 유사 상품 탐색
- 쇼핑몰 검색 시간을 줄이는 빠른 추천 경험
- 코디를 아이템 단위로 나누어 더 실용적인 결과 제공
- 개인이 사용하는 Private 웹 서비스로 가볍고 집중도 높은 운영 가능

---

## 2. 주요 기능

### 2.1 코디/착장 이미지 업로드
- 사용자가 코디 또는 착장 이미지를 업로드
- 업로드된 이미지를 기반으로 스타일 및 의류 요소 분석
- 웹 환경에서 간단하게 업로드 가능한 UX 제공

### 2.2 유사 상품 추천
- 업로드한 이미지와 유사한 의류 상품을 지그재그와 29CM에서 탐색
- 이미지 유사도 기반으로 추천 결과 생성
- 사용자가 직접 검색어를 입력하지 않아도 추천 결과 확인 가능

### 2.3 카테고리별 상품 분류
추천 상품을 다음과 같은 카테고리로 나누어 표시합니다.
- 상의
- 하의
- 아우터
- 신발
- 가방
- 기타 액세서리(확장 가능)

### 2.4 유사도순 정렬
- 추천 상품은 기본적으로 유사도순으로 정렬
- 사용자는 가장 비슷한 상품부터 확인 가능

### 2.5 가격대 필터
- 최소 가격 ~ 최대 가격 범위 설정 가능
- 예산에 맞는 상품만 빠르게 필터링 가능

### 2.6 찜 기능
- 마음에 드는 상품을 찜 목록에 저장
- 찜한 상품만 따로 모아 다시 확인 가능
- 로컬 단일 사용자 기준 저장 기능 제공

---

## 3. 플랫폼 및 서비스 범위

### 플랫폼
- 웹(Web)

### 서비스 범위
- Private 서비스
- 로그인 절차 없이 로컬 단일 사용자가 바로 이용 가능
- 퍼블릭 오픈 서비스가 아닌 내부/개인용 운영을 전제로 설계

---

## 4. 타겟 사용자

- SNS, 커뮤니티, 룩북 등에서 본 코디와 비슷한 상품을 빠르게 찾고 싶은 사용자
- 여러 쇼핑몰을 직접 검색하는 과정이 번거로운 사용자
- 코디 전체가 아니라 아이템 단위로 쇼핑하고 싶은 사용자
- 개인용 또는 소규모 테스트 환경에서 서비스를 운영하려는 관리자

---

## 5. 사용자 흐름

1. 사용자가 웹 서비스에 접속
2. 코디/착장 이미지를 업로드
3. 시스템이 이미지 속 패션 아이템을 분석
4. 지그재그와 29CM에서 유사 상품을 탐색
5. 추천 결과를 카테고리별로 분류하여 표시
6. 사용자는 유사도순으로 상품을 확인
7. 가격대 필터를 적용해 결과를 좁힘
8. 원하는 상품을 찜 목록에 저장
9. 찜 페이지에서 저장한 상품을 다시 확인

---

## 6. 서비스 화면 구성 예시

### 6.1 메인/업로드 페이지
- 서비스 소개
- 이미지 업로드 영역
- 현재 업로드 기준 분석 요약 또는 예시 이미지 노출 가능

### 6.2 추천 결과 페이지
- 원본 업로드 이미지 표시
- 카테고리 탭(상의/하의/아우터/신발/가방 등)
- 유사도순 정렬 결과
- 가격대 필터 UI
- 상품 카드(이미지, 상품명, 가격, 쇼핑몰명, 링크, 찜 버튼)

### 6.3 찜 목록 페이지
- 찜한 상품 리스트
- 카테고리별 보기
- 쇼핑몰별 보기
- 찜 해제 기능

### 6.4 로그인/사용자 관리 페이지
- 별도 로그인 없이 바로 사용하는 로컬 모드 안내
- 업로드 이력과 찜 흐름 중심의 단순 사용성 유지

---

## 7. 추천 로직 개요

서비스는 다음 흐름으로 동작합니다.

1. **이미지 업로드**
   - 사용자가 코디 이미지를 업로드합니다.

2. **이미지 분석**
   - 이미지 내 주요 패션 아이템을 식별합니다.
   - 필요 시 상의/하의/아우터/신발/가방 등으로 카테고리 분류를 수행합니다.

3. **특징 추출**
   - 이미지 임베딩 또는 비전 모델을 활용해 스타일 특징을 벡터화합니다.

4. **상품 데이터 탐색**
   - 지그재그와 29CM의 상품 이미지 및 메타데이터를 기반으로 유사 상품을 검색합니다.

5. **유사도 계산 및 랭킹**
   - 시각적 유사도, 카테고리 일치 여부, 가격 정보 등을 반영해 결과를 정렬합니다.

6. **결과 제공**
   - 카테고리별로 상품을 나누어 보여주고, 가격 필터 및 찜 기능을 제공합니다.

---

## 8. 기술 구성 예시

아래는 구현 시 고려할 수 있는 예시 스택입니다.

### 프론트엔드
- Next.js
- React
- TypeScript

### 백엔드
- FastAPI 또는 NestJS
- REST API 기반 서비스 구성

### AI / 추천
- CLIP 계열 이미지 임베딩 모델
- 패션 특화 분류/검출 모델
- 유사도 검색을 위한 벡터 검색 로직

### 데이터 저장
- PostgreSQL
- pgvector 또는 별도 벡터 검색 엔진
- Redis(캐시, 세션 등)

### 파일 저장
- AWS S3 또는 이에 준하는 오브젝트 스토리지

### 접근 제어
- 로컬 단일 사용자 모드
- 필요 시 추후 이메일 로그인 또는 OAuth 확장 가능

---

## 9. 핵심 데이터 구조 예시

### LocalUser
- id
- mode (`single-user`)
- created_at

### UploadedImage
- id
- user_id
- image_url
- created_at

### Product
- id
- source (`zigzag`, `29cm`)
- product_name
- category
- price
- product_url
- image_url
- embedding
- updated_at

### RecommendationResult
- id
- uploaded_image_id
- product_id
- category
- similarity_score
- rank

### Wishlist
- id
- user_id
- product_id
- created_at

---

## 10. MVP 범위

초기 버전에서는 아래 기능에 집중합니다.

- 코디 이미지 1장 업로드
- 지그재그 / 29CM 기반 유사 상품 추천
- 카테고리별 결과 분류
- 유사도순 정렬
- 가격대 필터
- 찜 기능
- 로그인 없는 로컬 단일 사용자 모드

---

## 11. 향후 확장 아이디어

- 색상/브랜드/스타일 태그 필터
- 유사 상품 외에 “같이 코디하면 좋은 상품” 추천
- 사용자 취향 기반 개인화 추천
- 검색 결과 저장 및 히스토리 관리
- 상품 품절 여부 반영
- 남성/여성/유니섹스 스타일 구분
- 모바일 웹 최적화
- 브라우저 확장 또는 저장 링크 기반 추천 기능

---

## 12. 운영 시 고려사항

### 외부 쇼핑몰 연동
지그재그와 29CM의 상품 데이터를 활용할 때는 반드시 다음을 확인해야 합니다.
- 공식 API 제공 여부
- 제휴 가능 여부
- 이용약관 및 robots 정책
- 허용된 범위 내 데이터 수집/노출 정책

### 개인정보 및 이미지 처리
- 업로드 이미지는 필요한 범위 내에서만 저장
- 사용자 이미지 삭제 기능 제공 권장
- 접근 제어 및 저장 데이터 암호화 고려

### 추천 품질
- 단순 이미지 유사도만으로는 정확도가 떨어질 수 있으므로
  카테고리 분류, 색상, 실루엣, 패턴 등의 보조 신호를 함께 반영하는 방식이 바람직합니다.

---

## 13. 기대 효과

- 사용자는 코디 이미지 한 장으로 빠르게 쇼핑 가능한 상품을 찾을 수 있습니다.
- 여러 쇼핑몰을 오가며 검색하는 시간을 줄일 수 있습니다.
- 코디 단위 이미지를 실제 구매 가능한 아이템 단위 결과로 연결할 수 있습니다.
- Private 환경에서 먼저 추천 품질과 사용자 경험을 검증할 수 있습니다.

---

## 14. 한 줄 요약

**스타일매치는 로그인 없이 코디 이미지를 업로드하면, 지그재그와 29CM에서 유사한 의류 상품을 카테고리별로 추천해주는 개인용 웹 서비스입니다.**


## 42. 프론트 메뉴 및 페이지 타이틀 한글화 메모
- PLAN ID: `PLAN-20260421-프론트메뉴및페이지타이틀한글화`
- 메뉴/페이지 대표 타이틀과 주요 상단 라벨을 한글로 통일했습니다.


## 43. 추천 목데이터 이미지 노출 보강 메모
- PLAN ID: `PLAN-20260421-추천목데이터이미지노출보강`
- 현재 29CM/지그재그 실 API가 아니라 백엔드 목데이터로 추천을 구성합니다.
- `example.com` 이미지 URL은 프론트에서 즉시 대체 썸네일로 치환해 카드 썸네일이 항상 보이도록 했습니다.


## 44. 헤더 업로드 초기화 버튼 제거 메모
- PLAN ID: `PLAN-20260421-헤더업로드초기화제거`
- 헤더의 `업로드 초기화` 버튼을 제거해 상단을 메뉴 중심으로 단순화했습니다.


## 45. 네이버 쇼핑 검색 연동 메모
- PLAN ID: `PLAN-20260421-네이버쇼핑검색연동`
- 29CM/지그재그 대신 네이버 쇼핑 검색 API를 추천 후보 조회 소스로 추가했습니다.
- `NAVER_SHOPPING_CLIENT_ID`, `NAVER_SHOPPING_CLIENT_SECRET`이 설정되어 있으면 네이버 쇼핑 결과를 사용합니다.
- 키가 없거나 API 호출에 실패하면 기존 샘플 상품 데이터로 자동 fallback 합니다.

## 46. 네이버 쇼핑 연동 진단 표시 메모
- PLAN ID: `PLAN-20260422-네이버연동진단표시`
- 네이버 쇼핑 검색 API 호출이 실패하면 추천 응답에 `fallback_reason`, `fallback_message`가 포함됩니다.
- 추천 페이지는 네이버 인증 실패/네트워크 오류/응답 오류를 안내하고, 화면은 기존 샘플 데이터로 계속 표시합니다.
- 현재 로컬 진단에서는 네이버 응답이 `401 인증 실패`로 확인되어 Client ID/Secret 또는 검색 API 사용 설정 확인이 필요합니다.

## 47. 최근 업로드 실제 썸네일 표시 메모
- PLAN ID: `PLAN-20260422-최근업로드실제썸네일`
- 과거에는 최근 업로드 패널용 썸네일 저장을 사용했습니다.
- 이후 `PLAN-20260504-최근업로드이미지재사용복원`에서 최근 업로드 이미지를 브라우저 `IndexedDB`에 다시 저장하고 있습니다.

## 48. 업로드 이미지 실제 응답 보강 메모
- PLAN ID: `PLAN-20260422-업로드이미지서빙보강`
- 업로드 응답의 `image_url`을 `/images/{uploaded_image_id}/file`로 변경해 실제 이미지 bytes를 받을 수 있게 했습니다.
- 현재 프론트 핵심 흐름은 업로드 직후 추천 이동이며, 응답 이미지 경로는 서버 측 업로드 식별자와 함께 유지됩니다.
- 백엔드 재시작 시 메모리 업로드 파일은 사라질 수 있으므로 새 업로드부터 적용됩니다.

## 49. 추천 카테고리 다양화 메모
- PLAN ID: `PLAN-20260422-추천카테고리다양화`
- 추천 전체 보기에서는 네이버 쇼핑 검색을 `상의`, `하의`, `아우터`, `신발`, `가방`으로 분산 호출합니다.
- 특정 카테고리 필터를 선택하면 해당 카테고리만 조회하는 기존 흐름을 유지합니다.
- 실제 로컬 검증에서 네이버 결과가 `top`, `bottom`, `outer`, `shoes`, `bag`으로 함께 표시되는 것을 확인했습니다.

## 50. 이미지 색상 기반 추천 정확도 개선 메모
- PLAN ID: `PLAN-20260423-이미지색상기반추천정확도개선`
- 업로드 이미지의 실제 픽셀을 샘플링해 `dominant_color`를 분석합니다.
- 네이버 자동 검색어에 대표 색상 키워드를 추가해 더 비슷한 색상의 상품 후보를 가져옵니다.
- 추천 점수에 `color_bonus`를 추가하고, 프론트 매칭 정보에서 색상 신호와 보너스를 확인할 수 있습니다.
- Pillow가 없거나 이미지 파싱에 실패하면 기존 fallback 흐름으로 계속 동작합니다.

## 51. 상품 이미지 직접 분석 추천 정확도 개선 메모
- PLAN ID: `PLAN-20260423-상품이미지직접분석추천정확도개선`
- 네이버 쇼핑 상품 이미지 URL을 짧은 timeout으로 다운로드해 대표 색상과 이미지 feature vector를 분석합니다.
- 상품 이미지 대표 색상이 업로드 이미지 대표 색상과 같으면 `product_image_color_bonus`가 추천 점수에 반영됩니다.
- 상품 이미지 분석 실패 시 상품명 색상 추론과 기존 hash vector fallback을 사용합니다.
- 환경값 `NAVER_SHOPPING_ANALYZE_PRODUCT_IMAGES`, `NAVER_SHOPPING_IMAGE_TIMEOUT_SECONDS`, `NAVER_SHOPPING_MAX_IMAGE_BYTES`, `NAVER_SHOPPING_MAX_PRODUCT_IMAGE_ANALYSIS_COUNT`로 동작을 조정할 수 있습니다.

## 52. 카테고리별 코디 검색어 추출 메모
- PLAN ID: `PLAN-20260423-카테고리별코디검색어추출`
- 업로드 이미지의 edge 색상으로 배경을 추정하고, 배경과 가까운 픽셀을 제외해 코디 구성품 색상을 읽습니다.
- 카테고리별 검색어 힌트를 우선 사용해 `화이트 셔츠 / 화이트 팬츠 / 블랙 니트 베스트 / 브라운 메리제인 슈즈 / 아이보리 숄더백`처럼 구성품별 검색어를 생성합니다.
- 실제 `codytest_01.png` 기준으로 베이지 전체 검색어 대신 품목별 검색어가 생성되는 것을 확인했습니다.

## 53. 검색어 하드코딩 제거 메모
- PLAN ID: `PLAN-20260423-검색어하드코딩제거`
- 카테고리별 검색어는 기본적으로 이미지에서 읽은 색상과 카테고리명을 조합해 동적으로 생성합니다.
- `codytest_01.png`처럼 흰 셔츠/검은 베스트/흰 팬츠 조합이 명확한 경우에만 구체 품목명을 유지합니다.
- 회귀 테스트에서 다른 색상 코디 이미지는 `레드 상의 / 블루 팬츠 / 그린 아우터 / 블랙 신발 / 옐로우 가방`처럼 별도 검색어로 생성되는 것을 확인했습니다.

## 54. 최신 업로드 추천 동기화 메모
- PLAN ID: `PLAN-20260424-AI비전기반착장분석도입`
- 업로드는 추천 페이지 이동 시 최신 `uploaded_image_id`를 URL에 함께 전달합니다.
- 추천 fallback은 기본 샘플 카탈로그만 사용해서, 이전 업로드에서 등록된 외부 상품 후보가 다음 업로드 추천에 섞이지 않도록 분리했습니다.
